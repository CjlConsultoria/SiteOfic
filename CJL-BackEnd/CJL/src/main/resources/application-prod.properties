spring.application.name=CJL
server.port=8080

# PostgreSQL (Render)
# Render fornece DATABASE_URL no formato postgresql://user:pass@host/db
# Mas o JDBC precisa de jdbc:postgresql://host:5432/db com user/pass separados
# Por isso usamos JDBC_DATABASE_URL ou construímos manualmente
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://dpg-d7ajsobuibrs739scsm0-a.oregon-postgres.render.com:5432/cjl_rxgo}
spring.datasource.username=${DATABASE_USER:cjlprod}
spring.datasource.password=${DATABASE_PASSWORD:2rmZKHviKluhwt1J7oSOt3biHuiWVkKR}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# H2 console desabilitado
spring.h2.console.enabled=false

# JPA
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update

# JWT
jwt.secret=${JWT_SECRET:minha-chave-secreta-super-segura-com-mais-de-64-caracteres!!!1234567890}
